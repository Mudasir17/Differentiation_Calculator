#include<iostream>
#include<regex>
#include<string>
using namespace std;
int main(){
	string exp;
	cout<<"Enter the expression "<<endl;
	getline(cin,exp)
	regex parse(R"((\d+(\.\d+)?|[a-zA-Z]\w+)*)");
	
	
	
	
	
}
